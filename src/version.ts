// 8Router — Central Version
// Single source of truth for version string across CLI, TUI, API, landing page.
// Update ONLY here + package.json on release.

export const VERSION = '1.0.0-rc.1';
export const VERSION_STRING = `v${VERSION}`;
export const RELEASE_NAME = 'Phase 5C-5F Production Hardening RC';
